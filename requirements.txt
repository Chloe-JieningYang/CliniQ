# PyTorch 2.7.1+cu118 (GPU: --extra-index-url https://download.pytorch.org/whl/cu118)
torch==2.7.1
torchvision==0.22.1
torchaudio==2.7.1

# Hugging Face & training
transformers>=5.0.0,<6.0.0
datasets>=4.0.0,<5.0.0
accelerate>=1.4.0,<2.0.0
trl>=0.29.0,<0.30.0
peft>=0.18.0,<0.19.0
bitsandbytes>=0.49.0
huggingface_hub>=1.0.0

# Config & logging
python-dotenv
tensorboard

# Evaluation
bert-score
sacrebleu
rouge-score

# RAG
langchain
langchain-core
langchain-community
langchain-huggingface
langchain-text-splitters
# faiss-gpu has no reliable pip wheels for many Python/OS combos; faiss-cpu installs everywhere.
# GPU Faiss (optional): use conda-forge `faiss-gpu` in a separate env, or build from source.
faiss-cpu
sentence-transformers
tiktoken

# Optional: Jupyter
jupyter

# CLI
fire

# API server
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.32.0,<1.0.0
pydantic-settings>=2.6.0,<3.0.0